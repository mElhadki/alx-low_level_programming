#include <stdio.h>
/**
 *main - Prints a sentence with proper grammar followed by a new line
 *Return: 0 if there are no errors, non-zero if there are errors.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
